# Raspberry Pi 4 Production Suite Dependencies

# Base Math & Array Operations
numpy>=1.24.0

# Hardware GPIO Pin Control
RPi.GPIO>=0.7.1

# OpenCV for Image/Camera Acquisition
opencv-python-headless>=4.8.0

# Deep Learning / YOLOv8 Segmentation
ultralytics>=8.0.0

# High-Performance CPU Optimization (Optional but Recommended for RPi 4)
# Run `model.export(format='onnx')` and use this for 3x faster CPU execution
onnxruntime>=1.15.0
